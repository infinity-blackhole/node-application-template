{
  "name": "my-hello-world",
  "version": "0.1.0",
  "description": "My Hello World",
  "main": "dist/index.js",
  "repository": "git@github.com:infinity-blackhole/my-hello-world.git",
  "author": "Black Hole <void@example.com>",
  "license": "MIT",
  "scripts": {
    "start": "node dist/app.js",
    "start:watch": "nodemon --watch dist dist/app.js",
    "check": "yarn tsc",
    "build": "swc src --config-file swc.config.json --out-dir dist",
    "build:watch": "npm run build -- --watch",
    "test": "jest",
    "test:watch": "npm run test -- --watchAll",
    "format": "prettier -w .",
    "format:watch": "npm run format -- -w"
  },
  "devDependencies": {
    "@swc/cli": "^0.1.55",
    "@swc/core": "^1.2.146",
    "@swc/jest": "^0.2.20",
    "@types/jest": "^27.4.1",
    "@types/node": "^17.0.21",
    "chokidar": "^3.5.3",
    "jest": "^27.5.1",
    "nodemon": "^2.0.15",
    "prettier": "^2.5.1",
    "typescript": "^4.5.5"
  }
}
